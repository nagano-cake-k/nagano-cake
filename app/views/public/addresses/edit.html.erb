<h4>配送先編集</h4>

<%= render 'error', address: @address %>

<%= form_with model: @address, url: address_path, method: :patch do |f| %>

<table>
    <tr>
        <td><%= f.label :post_code, "住所(ハイフンなし)" %></td><td><%= f.text_field :post_code, placeholder: "0000000" %></td><td><br /></td>
    </tr>
    <tr>
        <td><%= f.label :address, "住所" %></td><td><%= f.text_field :address, placeholder: "東京都渋谷区代々木神園町0-0"%></td><td><br /></td>
    </tr>
    <tr>
        <td> <%= f.label :name, "宛名" %></td><td><%= f.text_field :name, placeholder: "令和道子" %></td><td><%= f.submit '変更内容を保存する', class: "btn btn-success" %></td>
    </tr>
</table>
<% end %>

<%= link_to "戻る", addresses_path, class: "btn btn-primary" %>